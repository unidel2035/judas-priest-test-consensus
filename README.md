# Fullstack Чат с сохранением истории

Fullstack приложение для чата с использованием Node.js на бэкенде, Vue.js на фронтенде и SQLite для хранения истории сообщений.

## 🚀 Возможности

- 💬 Обмен сообщениями в реальном времени через WebSocket
- 💾 Сохранение истории сообщений в SQLite базе данных
- 👥 Поддержка нескольких пользователей одновременно
- 🔄 Автоматическое переподключение при потере связи
- 📱 Адаптивный дизайн
- 🎨 Современный UI с градиентами и анимациями

## 🏗️ Архитектура

### Backend (Node.js)
- **Express.js** - веб-фреймворк
- **WebSocket (ws)** - для real-time коммуникации
- **better-sqlite3** - для работы с SQLite базой данных
- **CORS** - для кросс-доменных запросов

### Frontend (Vue.js 3)
- **Vue 3** - прогрессивный JavaScript фреймворк
- **Vite** - быстрый инструмент сборки
- **WebSocket API** - для подключения к серверу

### База данных
- **SQLite** - легковесная встраиваемая база данных
- Схема таблицы `messages`:
  - `id` - автоинкрементный первичный ключ
  - `username` - имя пользователя
  - `message` - текст сообщения
  - `timestamp` - время отправки сообщения

## 📋 Требования

- Node.js >= 16.0.0
- npm >= 8.0.0

## ⚙️ Установка и запуск

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd test-consensus
```

### 2. Установка зависимостей

#### Backend
```bash
cd backend
npm install
```

#### Frontend
```bash
cd frontend
npm install
```

### 3. Запуск приложения

Вам потребуется два терминала - один для бэкенда, другой для фронтенда.

#### Терминал 1: Запуск бэкенда
```bash
cd backend
npm start
```

Сервер запустится на `http://localhost:3000`

Для режима разработки с автоматической перезагрузкой:
```bash
npm run dev
```

#### Терминал 2: Запуск фронтенда
```bash
cd frontend
npm run dev
```

Приложение будет доступно на `http://localhost:5173`

### 4. Использование

1. Откройте браузер и перейдите на `http://localhost:5173`
2. Введите ваше имя в поле "Ваше имя"
3. Введите сообщение в поле ввода
4. Нажмите "Отправить" или клавишу Enter
5. Откройте приложение в нескольких вкладках/окнах для тестирования real-time функциональности

## 🔌 API Endpoints

### WebSocket
- `ws://localhost:3000` - WebSocket подключение для real-time чата

### REST API
- `GET /api/messages` - получить все сообщения из истории
- `DELETE /api/messages` - удалить все сообщения
- `GET /api/health` - проверка состояния сервера

## 📁 Структура проекта

```
test-consensus/
├── backend/
│   ├── server.js           # Основной файл сервера
│   ├── package.json        # Зависимости бэкенда
│   └── chat.db            # SQLite база данных (создается автоматически)
├── frontend/
│   ├── src/
│   │   ├── App.vue        # Главный компонент Vue
│   │   └── main.js        # Точка входа приложения
│   ├── index.html         # HTML шаблон
│   ├── vite.config.js     # Конфигурация Vite
│   └── package.json       # Зависимости фронтенда
├── .gitignore             # Игнорируемые файлы
└── README.md              # Документация
```

## 🛠️ Режим разработки

### Backend
```bash
cd backend
npm run dev  # Использует nodemon для автоматической перезагрузки
```

### Frontend
```bash
cd frontend
npm run dev  # Vite HMR (Hot Module Replacement)
```

## 📦 Сборка для продакшена

### Frontend
```bash
cd frontend
npm run build
```

Собранные файлы будут в папке `frontend/dist/`

## 🔒 Безопасность

- Текущая реализация предназначена для демонстрационных целей
- Для продакшена рекомендуется добавить:
  - Аутентификацию пользователей
  - Валидацию входных данных
  - Rate limiting
  - HTTPS
  - Санитизацию сообщений

## 🐛 Устранение неполадок

### WebSocket не подключается
- Убедитесь, что бэкенд запущен на порту 3000
- Проверьте, что порт не занят другим приложением
- Проверьте URL в `frontend/src/App.vue` (должен быть `ws://localhost:3000`)

### База данных не создается
- Убедитесь, что у приложения есть права на запись в директорию `backend/`
- Проверьте, что SQLite корректно установлен через npm

### Сообщения не отправляются
- Убедитесь, что введено имя пользователя
- Проверьте соединение WebSocket (индикатор должен быть зеленым)
- Откройте консоль браузера для просмотра ошибок

## 📝 Лицензия

MIT

## 👨‍💻 Автор

Создано для решения задачи fullstack чат-приложения.
